# direnv configuration for cookido-agent
# Copy this file to .envrc and customize as needed
# This file is loaded by direnv when entering the directory

# Activate uv's virtual environment
# uv will create .venv when you run `uv sync`
if [[ -d .venv ]]; then
    source .venv/bin/activate
fi

# Environment variables for Cookidoo credentials
# Copy these to .envrc.local and fill in your values
# export COOKIDOO_USERNAME=""
# export COOKIDOO_PASSWORD=""
# export COOKIDOO_REGION="eu"  # Options: eu, us, etc.

# Load local overrides (gitignored)
if [[ -f .envrc.local ]]; then
    source .envrc.local
fi
